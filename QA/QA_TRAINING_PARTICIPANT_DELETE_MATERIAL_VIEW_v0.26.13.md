# v0.26.13 — Training participant delete + material view

- Added participant delete action with confirmation.
- Physical deletion is allowed only before attendance/completion/assessment evidence exists.
- Existing evidence blocks deletion to preserve history.
- Material View now uses an in-app preview.
- Images and PDFs preview inline.
- New demo uploads up to 4 MB persist a data URL so View works after reload.
- Older stale demo blob references show a re-upload message instead of a dead View action.
