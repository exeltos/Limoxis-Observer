# v0.25.4 — Single contextual Back

The shared EntityRecordShell now renders exactly one Back control.
It is positioned at the left of the record header, before the record avatar.
The previous right-side Back control was removed.

This applies consistently to committee create/detail and every other screen using EntityRecordShell.
