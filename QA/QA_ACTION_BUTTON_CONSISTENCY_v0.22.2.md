# Action button consistency — v0.22.2

- Controls no longer use a special dark `+ Νέος έλεγχος` CTA inside the registry surface.
- Create action moved to the Page top-right action area, matching Prevention and the rest of the platform.
- Uses the shared `record-actions` / `action-button` visual language and the common localized `create` label.
- Existing Controls role logic remains unchanged: only permitted roles see the create action.
