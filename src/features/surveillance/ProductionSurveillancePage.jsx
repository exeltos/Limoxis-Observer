import { useEffect,useMemo,useState } from 'react'
import { Activity,AlertTriangle,Clock3,Microscope } from 'lucide-react'
import { useLocation,useNavigate } from 'react-router-dom'
import { Page } from '../../design-system/Page'
import { Button } from '../../design-system/Button'
import { RecordActions } from '../../design-system/RecordActions'
import { FilterBar,FilterSelect } from '../../design-system/FilterBar'
import { ObserverDialog } from '../../design-system/ObserverDialog'
import { SaveButton } from '../../design-system/SaveButton'
import { ManualDateField } from '../../design-system/ManualDateField'
import { MetricCard } from '../../design-system/MetricCard'
import { RegistryPagination } from '../../design-system/RegistryPagination'
import { RegistryTable } from '../../design-system/RegistryTable'
import { UI_ACTIONS } from '../../core/actions/actionPolicy'
import { CAPABILITIES,ROLES,can } from '../../core/permissions/roles'
import { useRegistryMemory } from '../../core/navigation/useRegistryMemory'
import { useTenant } from '../../core/tenant/TenantContext'
import { useLanguage } from '../../core/i18n/LanguageContext'
import { useFeedback } from '../../core/feedback/FeedbackContext'
import { createPatient,loadPatients } from '../patients/patientsService'
import { NewPatientCard } from '../patients/PatientsPage'
import { loadDepartments } from '../management/departmentsService'
import { createLaboratorySample,loadLaboratorySamples,sampleTypeLabel } from '../laboratory/laboratoryCloudService'
import { createClinicalCase,loadClinicalCases } from './clinicalCloudService'
import { loadEmployeeSurveillanceBatches,loadEmployeeSurveillanceRecords,getEmployeeSurveillanceKpis } from './employeeSurveillanceCloudService'
import { ProductionEmployeeSurveillanceFlow } from './ProductionEmployeeSurveillanceFlow'
import { EmployeeSurveillanceRecordDialog } from './EmployeeSurveillanceRecordDialog'

const today=()=>new Date().toISOString().slice(0,10)
const reviewState=row=>row.status!=='active'?'completed':row.reviewDue&&new Date(`${row.reviewDue}T23:59:59`)<new Date()?'overdue':'inProgress'
const latestOrganism=row=>row.samples?.find(sample=>sample.organism)?.organism||null
const latestResistance=row=>row.samples?.find(sample=>sample.resistance)?.resistance||null
const uniqueSorted=values=>[...new Set(values.filter(Boolean))].sort((a,b)=>String(a).localeCompare(String(b),'el'))
const environmentalTypes=['water','surface','environment','environmental','room','air','νερό','επιφάνεια','επιφανεια']
const environmentalMethodsByCategory={surface:['surfaceSwab','contactPlate'],room:['roomSampling'],air:['activeAir','passiveAir'],water:['waterSampling']}
const pendingSampleStatuses=['requested','collected','received','processing']
const isEnvironmentalSample=row=>row.subjectType==='environment'||environmentalTypes.some(value=>String(row.type||'').toLowerCase().includes(value))
const patientLabCode=caseId=>`LAB-SUR-${String(caseId||'').replaceAll('-','')}`

export function ProductionSurveillancePage(){
  const {tenant,role,membership,canAccessRecord,canSeeSensitiveEmployeeHealth}=useTenant()
  const {t,language,locale}=useLanguage()
  const {notify,notifyError}=useFeedback()
  const navigate=useNavigate()
  const location=useLocation()
  const restored=location.state?.surveillanceView||{}
  const [records,setRecords]=useState([]),[patients,setPatients]=useState([]),[labSamples,setLabSamples]=useState([]),[employeeRecords,setEmployeeRecords]=useState([]),[employeeBatches,setEmployeeBatches]=useState([]),[departmentOptions,setDepartmentOptions]=useState([]),[loading,setLoading]=useState(true)
  const [query,setQuery]=useState(restored.query||''),[department,setDepartment]=useState(restored.department||'all'),[resistance,setResistance]=useState(restored.resistance||'all'),[review,setReview]=useState(restored.review||'all')
  const [environmentQuery,setEnvironmentQuery]=useState(restored.environmentQuery||''),[environmentType,setEnvironmentType]=useState(restored.environmentType||'all'),[environmentDepartment,setEnvironmentDepartment]=useState(restored.environmentDepartment||'all'),[environmentStatus,setEnvironmentStatus]=useState(restored.environmentStatus||'all')
  const [employeeQuery,setEmployeeQuery]=useState(restored.employeeQuery||''),[employeeDepartment,setEmployeeDepartment]=useState(restored.employeeDepartment||'all'),[employeeStatus,setEmployeeStatus]=useState(restored.employeeStatus||'all')
  const [registryMode,setRegistryMode]=useState(restored.registryMode||'patients'),[page,setPage]=useState(restored.page||1),[pageSize,setPageSize]=useState(restored.pageSize||15),[creationMode,setCreationMode]=useState(null),[saving,setSaving]=useState(false),[newPatientOpen,setNewPatientOpen]=useState(false)
  const [draft,setDraft]=useState({patientId:'',startedAt:today(),reviewDue:'',room:'',reason:''})
  const [environmentDraft,setEnvironmentDraft]=useState({type:'surface',environmentalMethod:'surfaceSwab',departmentId:'',source:'',collectedAt:today(),priority:'routine'})
  const registry=useRegistryMemory(`surveillance-${registryMode}`)

  const canSeeEmployeeSurveillance=[ROLES.HOSPITAL_ADMIN,ROLES.INFECTION_CONTROL_LEAD,ROLES.INFECTION_CONTROL_MEMBER,ROLES.OCCUPATIONAL_PHYSICIAN].includes(role)||Boolean(canSeeSensitiveEmployeeHealth)
  const canSeeEnvironmental=![ROLES.DEPARTMENT_MANAGER,ROLES.DEPARTMENT_USER,ROLES.DOCTOR_REVIEWER].includes(role)
  const canManageEmployeeFollowup=can(role,CAPABILITIES.MANAGE_OCCUPATIONAL_HEALTH,membership?.capabilities,membership?.customCapabilities)
  const [openEmployeeRecord,setOpenEmployeeRecord]=useState(null),[openEmployeeBatch,setOpenEmployeeBatch]=useState(null)

  async function load(){
    if(!tenant?.id){setRecords([]);setPatients([]);setLabSamples([]);setEmployeeRecords([]);setEmployeeBatches([]);setDepartmentOptions([]);setLoading(false);return}
    setLoading(true)
    try{
      const [casesResult,patientsResult,samplesResult,departmentsResult]=await Promise.allSettled([loadClinicalCases(tenant.id),loadPatients(tenant.id,{isDemo:false}),loadLaboratorySamples(tenant.id),loadDepartments(tenant.id)])
      if(casesResult.status==='fulfilled')setRecords(casesResult.value);else{setRecords([]);notifyError(casesResult.reason,'load',{operation:'surveillance_cases_load'})}
      if(patientsResult.status==='fulfilled')setPatients(patientsResult.value);else{setPatients([]);notifyError(patientsResult.reason,'load',{operation:'surveillance_patients_load'})}
      if(samplesResult.status==='fulfilled')setLabSamples(samplesResult.value);else{setLabSamples([]);notifyError(samplesResult.reason,'load',{operation:'surveillance_laboratory_load'})}
      if(departmentsResult.status==='fulfilled')setDepartmentOptions((departmentsResult.value||[]).filter(item=>item.is_active!==false).map(item=>({...item,nameEn:item.name})));else setDepartmentOptions([])
      if(canSeeEmployeeSurveillance){
        try{
          const employeeRows=await loadEmployeeSurveillanceRecords(tenant.id)
          const batchRows=await loadEmployeeSurveillanceBatches(tenant.id,employeeRows)
          setEmployeeRecords(employeeRows);setEmployeeBatches(batchRows)
        }catch(error){setEmployeeRecords([]);setEmployeeBatches([]);notifyError(error,'load',{operation:'employee_surveillance_registry_load'})}
      }else{setEmployeeRecords([]);setEmployeeBatches([])}
    }finally{setLoading(false)}
  }
  useEffect(()=>{void load()},[tenant?.id,canSeeEmployeeSurveillance])
  useEffect(()=>{if(!location.state?.surveillanceView)setPage(1)},[registryMode,pageSize,query,department,resistance,review,environmentQuery,environmentType,environmentDepartment,environmentStatus,employeeQuery,employeeDepartment,employeeStatus])

  const patientLabByCase=useMemo(()=>new Map(labSamples.filter(row=>row.surveillanceCase).map(row=>[row.surveillanceCase,row])),[labSamples])
  const employeeLabByRecord=useMemo(()=>{const map=new Map();labSamples.filter(row=>row.employeeSurveillanceId).forEach(row=>{const list=map.get(row.employeeSurveillanceId)||[];list.push(row);map.set(row.employeeSurveillanceId,list)});return map},[labSamples])
  const employeeLabsByBatch=useMemo(()=>{const map=new Map();labSamples.filter(row=>row.employeeSurveillanceBatchId).forEach(row=>{const list=map.get(row.employeeSurveillanceBatchId)||[];list.push(row);map.set(row.employeeSurveillanceBatchId,list)});return map},[labSamples])
  const environmental=useMemo(()=>labSamples.filter(isEnvironmentalSample),[labSamples])
  const departments=useMemo(()=>uniqueSorted(records.map(row=>language==='el'?row.department:row.departmentEn)),[records,language])
  const environmentalTypesList=useMemo(()=>uniqueSorted(environmental.map(row=>row.type)),[environmental])
  const environmentalDepartments=useMemo(()=>uniqueSorted(environmental.map(row=>row.department)),[environmental])
  const environmentalStatuses=useMemo(()=>uniqueSorted(environmental.map(row=>row.status)),[environmental])
  const employeeDepartments=useMemo(()=>uniqueSorted([...employeeRecords.map(row=>language==='en'?row.departmentEn:row.department),...employeeBatches.map(row=>language==='en'?row.departmentEn:row.department)]),[employeeRecords,employeeBatches,language])
  const employeeStatuses=useMemo(()=>uniqueSorted(employeeRecords.map(row=>row.resultStatus)),[employeeRecords])
  const patientRows=useMemo(()=>records.filter(row=>canAccessRecord(row)).filter(row=>`${row.patientId} ${row.patient} ${row.patientEn} ${latestOrganism(row)||''}`.toLowerCase().includes(query.trim().toLowerCase())).filter(row=>department==='all'||(language==='el'?row.department:row.departmentEn)===department).filter(row=>resistance==='all'||(resistance==='resistant'?Boolean(latestResistance(row)):!latestResistance(row))).filter(row=>review==='all'||reviewState(row)===review).map(row=>({...row,_labSample:patientLabByCase.get(row.recordId)||patientLabByCase.get(row.id)||null})),[records,canAccessRecord,query,department,resistance,review,language,patientLabByCase])
  const environmentalRows=useMemo(()=>environmental.filter(row=>`${row.type||''} ${row.source||''} ${row.department||''} ${row.organism||''}`.toLowerCase().includes(environmentQuery.trim().toLowerCase())).filter(row=>environmentType==='all'||row.type===environmentType).filter(row=>environmentDepartment==='all'||row.department===environmentDepartment).filter(row=>environmentStatus==='all'||row.status===environmentStatus),[environmental,environmentQuery,environmentType,environmentDepartment,environmentStatus])
  const employeeRows=useMemo(()=>employeeRecords.filter(row=>`${row.employeeId||''} ${row.employeeName||''} ${row.employeeNameEn||''} ${(row.screeningTypes||[]).join(' ')}`.toLowerCase().includes(employeeQuery.trim().toLowerCase())).filter(row=>employeeDepartment==='all'||(language==='en'?row.departmentEn:row.department)===employeeDepartment).filter(row=>employeeStatus==='all'||row.resultStatus===employeeStatus).map(row=>({...row,_registryKind:'individual',_labSamples:employeeLabByRecord.get(row.recordId)||[]})),[employeeRecords,employeeQuery,employeeDepartment,employeeStatus,language,employeeLabByRecord])
  const batchRows=useMemo(()=>employeeBatches.filter(row=>`${row.department||''} ${row.departmentEn||''} ${(row.screeningTypes||[]).join(' ')}`.toLowerCase().includes(employeeQuery.trim().toLowerCase())).filter(row=>employeeDepartment==='all'||(language==='en'?row.departmentEn:row.department)===employeeDepartment).filter(()=>employeeStatus==='all').map(row=>({...row,_registryKind:'bulk',_labSamples:employeeLabsByBatch.get(row.recordId)||[]})),[employeeBatches,employeeQuery,employeeDepartment,employeeStatus,language,employeeLabsByBatch])
  const unifiedEmployeeRows=useMemo(()=>[...employeeRows,...batchRows].sort((a,b)=>String(b.startedAt||'').localeCompare(String(a.startedAt||''))),[employeeRows,batchRows])
  const active=records.filter(row=>row.status==='active').length,due=records.filter(row=>reviewState(row)==='overdue').length,isolation=records.filter(row=>row.isolation?.status==='active').length,resistant=records.filter(row=>Boolean(latestResistance(row))).length
  const employeeKpis=getEmployeeSurveillanceKpis(employeeRecords),employeeMode=registryMode==='employees'
  const environmentalKpis={active:environmental.filter(row=>pendingSampleStatuses.includes(row.status)).length,pendingLab:environmental.filter(row=>pendingSampleStatuses.includes(row.status)&&!row.result).length,positive:environmental.filter(row=>row.result==='positive').length,critical:environmental.filter(row=>row.critical).length}
  const fmt=value=>value?new Intl.DateTimeFormat(locale).format(new Date(`${String(value).slice(0,10)}T12:00:00`)):'—'
  const activeRows=registryMode==='environmental'?environmentalRows:registryMode==='employees'?unifiedEmployeeRows:patientRows
  const totalPages=Math.max(1,Math.ceil(activeRows.length/pageSize)),safePage=Math.min(page,totalPages),pagedRows=activeRows.slice((safePage-1)*pageSize,safePage*pageSize)
  const surveillanceView=()=>({registryMode,page:safePage,pageSize,query,department,resistance,review,environmentQuery,environmentType,environmentDepartment,environmentStatus,employeeQuery,employeeDepartment,employeeStatus})
  const openLinkedRecord=(path,id,orderedIds=[],state={})=>registry.openRecord(navigate,path,id,orderedIds,{state,returnState:{surveillanceView:surveillanceView()}})

  async function createPatientRecord(){
    const patient=patients.find(item=>item.id===draft.patientId);if(!patient||!draft.startedAt||!draft.reason.trim()||saving)return
    setSaving(true)
    try{
      const created=await createClinicalCase(tenant.id,patient.recordId,{startedAt:draft.startedAt,reviewDue:draft.reviewDue||null,room:draft.room.trim(),reason:draft.reason.trim(),departmentId:patient.departmentId||null})
      setRecords(current=>[created,...current]);setCreationMode(null);setDraft({patientId:'',startedAt:today(),reviewDue:'',room:'',reason:''});notify(t('surveillanceCreated'),'success')
      navigate(`/laboratory/${patientLabCode(created.recordId||created.id)}`,{state:{returnTo:'/surveillance',limoxisFrom:{pathname:'/surveillance',state:{surveillanceView:{...surveillanceView(),registryMode:'patients'}}}}})
    }catch(error){notifyError(error,'save',{operation:'surveillance_create'})}finally{setSaving(false)}
  }
  async function createInlinePatient(patientDraft){try{const {record:patient,list}=await createPatient(tenant?.id,patients,patientDraft,{isDemo:false});setPatients(list);setDraft(current=>({...current,patientId:patient.id,startedAt:current.startedAt||patient.admissionDate||today()}));setNewPatientOpen(false);notify(t('patientCreated'),'success')}catch(error){notify(error?.duplicateCode?t('patientCodeDuplicate'):(error?.message||t('patientSaveFailed')),'danger')}}
  async function createEnvironmentalRecord(){
    if(!environmentDraft.type||!environmentDraft.source.trim()||saving)return
    setSaving(true)
    try{
      const created=await createLaboratorySample(tenant?.id,null,{type:environmentDraft.type,environmentalMethod:environmentDraft.environmentalMethod,departmentId:environmentDraft.departmentId||null,source:environmentDraft.source.trim(),collectedAt:environmentDraft.collectedAt||null,priority:environmentDraft.priority,subjectType:'environment',subjectName:environmentDraft.source.trim(),subjectCode:'ENV'})
      setLabSamples(current=>[created,...current]);setCreationMode(null);setRegistryMode('environmental');setEnvironmentDraft({type:'surface',environmentalMethod:'surfaceSwab',departmentId:'',source:'',collectedAt:today(),priority:'routine'});notify(language==='en'?'Environmental surveillance saved.':'Η περιβαλλοντική επιτήρηση αποθηκεύτηκε.','success')
    }catch(error){notifyError(error,'save',{operation:'environmental_surveillance_create'})}finally{setSaving(false)}
  }

  return <Page fill className="production-surveillance-page" title={t('clinicalRecords.surveillanceCenter')} subtitle={t('surveillanceSubtitleV051')} actions={<RecordActions actions={[UI_ACTIONS.CREATE]} actionCapabilities={{[UI_ACTIONS.CREATE]:CAPABILITIES.CREATE_SURVEILLANCE}} onAction={action=>{if(action===UI_ACTIONS.CREATE)setCreationMode('chooser')}}/>}>
    <div className="workspace-summary surveillance-summary"><div className="module-summary-strip">{employeeMode?<><SummaryMetric icon={Activity} label={t('clinicalRecords.activeEmployeeScreenings')} value={employeeKpis.active}/><SummaryMetric icon={Microscope} label={t('clinicalRecords.positiveEmployeeScreenings')} value={employeeKpis.positive}/><SummaryMetric icon={AlertTriangle} label={t('clinicalRecords.needsIntervention')} value={employeeKpis.needsIntervention}/><SummaryMetric icon={Clock3} label={t('clinicalRecords.needsRecheck')} value={employeeKpis.needsRecheck}/></>:registryMode==='environmental'?<><SummaryMetric icon={Activity} label={t('clinicalRecords.activeEnvironmentalSampling')} value={environmentalKpis.active}/><SummaryMetric icon={Clock3} label={t('clinicalRecords.pendingEnvironmentalLab')} value={environmentalKpis.pendingLab}/><SummaryMetric icon={Microscope} label={t('clinicalRecords.positiveEnvironmentalPoints')} value={environmentalKpis.positive}/><SummaryMetric icon={AlertTriangle} label={language==='en'?'Critical findings':'Κρίσιμα ευρήματα'} value={environmentalKpis.critical}/></>:<><SummaryMetric icon={Activity} label={t('activeSurveillance')} value={active}/><SummaryMetric icon={Clock3} label={t('clinicalRecords.needsReview')} value={due}/><SummaryMetric icon={AlertTriangle} label={t('clinicalRecords.activeIsolation')} value={isolation}/><SummaryMetric icon={Microscope} label={t('clinicalRecords.mdrXdr')} value={resistant}/></>}</div></div>
    <div className="surface registry-workspace production-surveillance-frame workspace-fill">
      <nav className="tabs surveillance-domain-tabs canonical-module-tabs" aria-label={t('surveillanceCategoriesAria')}>
        <button type="button" className={`tab ${registryMode==='patients'?'active':''}`} onClick={()=>setRegistryMode('patients')}>{t('patients')}</button>
        {canSeeEmployeeSurveillance&&<button type="button" className={`tab ${registryMode==='employees'?'active':''}`} onClick={()=>setRegistryMode('employees')}>{t('employees')}</button>}
        {canSeeEnvironmental&&<button type="button" className={`tab ${registryMode==='environmental'?'active':''}`} onClick={()=>setRegistryMode('environmental')}>{t('clinicalRecords.environment')}</button>}
      </nav>
      <div className="surveillance-workspace production-surveillance-workspace">
        {registryMode==='patients'&&<FilterBar query={query} onQueryChange={setQuery} placeholder={t('clinicalRecords.searchSurveillance')} activeAdvancedCount={(department!=='all'?1:0)+(resistance!=='all'?1:0)+(review!=='all'?1:0)} onClear={()=>{setQuery('');setDepartment('all');setResistance('all');setReview('all')}} advanced={<><FilterSelect label={t('department')} value={department} onChange={setDepartment}><option value="all">{t('allDepartments')}</option>{departments.map(value=><option key={value} value={value}>{value}</option>)}</FilterSelect><FilterSelect label={t('clinicalRecords.resistance')} value={resistance} onChange={setResistance}><option value="all">{t('all')}</option><option value="resistant">{t('clinicalRecords.mdrXdr')}</option><option value="none">{t('clinicalRecords.noResistanceFlag')}</option></FilterSelect><FilterSelect label={t('reassessment')} value={review} onChange={setReview}><option value="all">{t('all')}</option><option value="overdue">{t('overdue')}</option><option value="inProgress">{t('inProgress')}</option><option value="completed">{t('completed')}</option></FilterSelect></>}/>} 
        {registryMode==='environmental'&&<FilterBar query={environmentQuery} onQueryChange={setEnvironmentQuery} placeholder={language==='en'?'Search source, department or microorganism…':'Αναζήτηση πηγής, τμήματος ή μικροοργανισμού…'} activeAdvancedCount={(environmentType!=='all'?1:0)+(environmentDepartment!=='all'?1:0)+(environmentStatus!=='all'?1:0)} onClear={()=>{setEnvironmentQuery('');setEnvironmentType('all');setEnvironmentDepartment('all');setEnvironmentStatus('all')}} advanced={<><FilterSelect label={language==='en'?'Type':'Τύπος'} value={environmentType} onChange={setEnvironmentType}><option value="all">{t('all')}</option>{environmentalTypesList.map(value=><option key={value} value={value}>{value}</option>)}</FilterSelect><FilterSelect label={t('department')} value={environmentDepartment} onChange={setEnvironmentDepartment}><option value="all">{t('allDepartments')}</option>{environmentalDepartments.map(value=><option key={value} value={value}>{value}</option>)}</FilterSelect><FilterSelect label={t('status')} value={environmentStatus} onChange={setEnvironmentStatus}><option value="all">{t('all')}</option>{environmentalStatuses.map(value=><option key={value} value={value}>{value}</option>)}</FilterSelect></>}/>} 
        {registryMode==='employees'&&canSeeEmployeeSurveillance&&<FilterBar query={employeeQuery} onQueryChange={setEmployeeQuery} placeholder={language==='en'?'Search employee surveillance…':'Αναζήτηση επιτήρησης εργαζομένων…'} activeAdvancedCount={(employeeDepartment!=='all'?1:0)+(employeeStatus!=='all'?1:0)} onClear={()=>{setEmployeeQuery('');setEmployeeDepartment('all');setEmployeeStatus('all')}} advanced={<><FilterSelect label={t('department')} value={employeeDepartment} onChange={setEmployeeDepartment}><option value="all">{t('allDepartments')}</option>{employeeDepartments.map(value=><option key={value} value={value}>{value}</option>)}</FilterSelect><FilterSelect label={t('status')} value={employeeStatus} onChange={setEmployeeStatus}><option value="all">{t('all')}</option>{employeeStatuses.map(value=><option key={value} value={value}>{value}</option>)}</FilterSelect></>}/>} 
        {loading?<div className="registry-empty-state"><strong>{t('loading')}</strong></div>:<>{registryMode==='patients'&&<PatientRegistry rows={pagedRows} totalRows={patientRows.length} t={t} language={language} fmt={fmt} registry={registry} openLinkedRecord={openLinkedRecord}/>} {registryMode==='environmental'&&<EnvironmentalRegistry rows={pagedRows} totalRows={environmentalRows.length} language={language} t={t} fmt={fmt} registry={registry} openLinkedRecord={openLinkedRecord}/>} {registryMode==='employees'&&canSeeEmployeeSurveillance&&<UnifiedEmployeeRegistry rows={pagedRows} totalRows={unifiedEmployeeRows.length} language={language} fmt={fmt} registry={registry} onOpenRecord={setOpenEmployeeRecord} onOpenBatch={setOpenEmployeeBatch}/>}<RegistryPagination language={language} page={safePage} totalPages={totalPages} totalItems={activeRows.length} pageSize={pageSize} onPageChange={setPage} onPageSizeChange={size=>{setPageSize(size);setPage(1)}}/></>}
      </div>
    </div>
    {creationMode==='chooser'&&<SubjectChooser language={language} canEmployee={canSeeEmployeeSurveillance} canEnvironmental={canSeeEnvironmental} onClose={()=>setCreationMode(null)} onPatient={()=>setCreationMode('patient')} onEmployee={()=>setCreationMode('employee')} onEnvironmental={()=>setCreationMode('environmental')}/>} 
    {creationMode==='patient'&&<ObserverDialog width="wide" eyebrow={language==='en'?'Patient surveillance':'Επιτήρηση ασθενούς'} title={language==='en'?'Start surveillance':'Έναρξη επιτήρησης'} subtitle={language==='en'?'Select an existing patient or create a new patient without leaving this workflow.':'Επιλέξτε υπάρχοντα ασθενή ή δημιουργήστε νέο χωρίς να φύγετε από τη ροή.'} onClose={()=>!saving&&setCreationMode(null)} footer={<SaveButton loading={saving} disabled={!draft.patientId||!draft.startedAt||!draft.reason.trim()} onClick={createPatientRecord}>{language==='en'?'Create surveillance':'Δημιουργία επιτήρησης'}</SaveButton>}><div className="entry-grid compact"><label className="field entry-span-2"><span>{t('patient')} *</span><select value={draft.patientId} onChange={event=>setDraft(current=>({...current,patientId:event.target.value}))}><option value="">{language==='en'?'Select patient…':'Επιλογή ασθενούς…'}</option>{patients.map(patient=><option key={patient.id} value={patient.id}>{patient.id} · {patient.name} · {patient.department||'—'}</option>)}</select></label><div className="entry-span-2"><Button variant="secondary" onClick={()=>setNewPatientOpen(true)}>{language==='en'?'+ New patient':'+ Νέος ασθενής'}</Button></div><ManualDateField label={`${language==='en'?'Start date':'Ημερομηνία έναρξης'} *`} value={draft.startedAt} onChange={value=>setDraft(current=>({...current,startedAt:value}))}/><ManualDateField label={language==='en'?'Review due':'Επανεκτίμηση έως'} value={draft.reviewDue} onChange={value=>setDraft(current=>({...current,reviewDue:value}))}/><label className="field"><span>{language==='en'?'Room':'Θάλαμος'}</span><input value={draft.room} onChange={event=>setDraft(current=>({...current,room:event.target.value}))}/></label><label className="field"><span>{language==='en'?'Reason / indication':'Αιτία / ένδειξη'} *</span><input value={draft.reason} onChange={event=>setDraft(current=>({...current,reason:event.target.value}))}/></label></div></ObserverDialog>}
    {creationMode==='environmental'&&<ObserverDialog width="wide" eyebrow={language==='en'?'Environmental surveillance':'Περιβαλλοντική επιτήρηση'} title={language==='en'?'New environmental sample':'Νέα περιβαλλοντική καταγραφή'} subtitle={language==='en'?'Record the sample here. Laboratory processing can continue from the same record afterwards.':'Καταχωρίστε εδώ το περιβαλλοντικό δείγμα. Η εργαστηριακή επεξεργασία μπορεί να συνεχιστεί αργότερα στην ίδια εγγραφή.'} onClose={()=>!saving&&setCreationMode(null)} footer={<SaveButton loading={saving} disabled={!environmentDraft.type||!environmentDraft.source.trim()} onClick={createEnvironmentalRecord}>{language==='en'?'Save surveillance':'Αποθήκευση επιτήρησης'}</SaveButton>}><div className="entry-grid compact"><label className="field"><span>{t('environmentalStandards.samplingCategory')} *</span><select value={environmentDraft.type} onChange={event=>{const category=event.target.value;setEnvironmentDraft(current=>({...current,type:category,environmentalMethod:environmentalMethodsByCategory[category][0]}))}}><option value="surface">{t('environmentalStandards.surface')}</option><option value="room">{t('environmentalStandards.roomCategory')}</option><option value="air">{t('environmentalStandards.air')}</option><option value="water">{t('environmentalStandards.water')}</option></select></label><label className="field"><span>{language==='en'?'Sampling method':'Μέθοδος δειγματοληψίας'} *</span><select value={environmentDraft.environmentalMethod} onChange={event=>setEnvironmentDraft(current=>({...current,environmentalMethod:event.target.value}))}>{environmentalMethodsByCategory[environmentDraft.type].map(method=><option key={method} value={method}>{t(`environmentalStandards.${method}`)}</option>)}</select></label><label className="field"><span>{t('department')}</span><select value={environmentDraft.departmentId} onChange={event=>setEnvironmentDraft(current=>({...current,departmentId:event.target.value}))}><option value="">{language==='en'?'Not assigned':'Χωρίς τμήμα'}</option>{departmentOptions.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="field entry-span-2"><span>{language==='en'?'Sampling point / source':'Σημείο δειγματοληψίας / πηγή'} *</span><input value={environmentDraft.source} onChange={event=>setEnvironmentDraft(current=>({...current,source:event.target.value}))} placeholder={language==='en'?'e.g. ICU bed rail, tap, operating room air':'π.χ. κάγκελο κλίνης ΜΕΘ, βρύση, αέρας χειρουργείου'}/></label><ManualDateField label={language==='en'?'Collection date':'Ημερομηνία συλλογής'} value={environmentDraft.collectedAt} onChange={value=>setEnvironmentDraft(current=>({...current,collectedAt:value}))}/><label className="field"><span>{t('priority')}</span><select value={environmentDraft.priority} onChange={event=>setEnvironmentDraft(current=>({...current,priority:event.target.value}))}><option value="routine">{t('routine')}</option><option value="urgent">{t('urgent')}</option><option value="critical">{t('critical')}</option></select></label></div></ObserverDialog>}
    {newPatientOpen&&<NewPatientCard t={t} language={language} departments={departmentOptions} onClose={()=>setNewPatientOpen(false)} onSave={createInlinePatient}/>}
    {creationMode==='employee'&&canSeeEmployeeSurveillance&&<ProductionEmployeeSurveillanceFlow onClose={()=>setCreationMode(null)} onCreated={async()=>{await load();setRegistryMode('employees')}}/>}
    {openEmployeeRecord&&<EmployeeSurveillanceRecordDialog organizationId={tenant?.id} record={openEmployeeRecord} samples={employeeLabByRecord.get(openEmployeeRecord.recordId)||[]} canManage={canManageEmployeeFollowup} t={t} language={language} fmt={fmt} onClose={()=>setOpenEmployeeRecord(null)} onUpdated={updated=>{setEmployeeRecords(current=>current.map(row=>row.recordId===updated.recordId?updated:row));setOpenEmployeeRecord(updated)}}/>}
    {openEmployeeBatch&&<EmployeeBatchDialog language={language} fmt={fmt} batch={openEmployeeBatch} onClose={()=>setOpenEmployeeBatch(null)} onOpenRecord={record=>{setOpenEmployeeBatch(null);setOpenEmployeeRecord(record)}}/>}
  </Page>
}

function EmployeeBatchDialog({language,fmt,batch,onClose,onOpenRecord}){
  const en=language==='en'
  return <ObserverDialog eyebrow={en?'Bulk surveillance':'Μαζική επιτήρηση'} title={batch.id} subtitle={`${fmt(batch.startedAt)} · ${(batch.screeningTypes||[]).join(', ')}`} width="wide" className="employee-batch-record-card" onClose={onClose}><div className="employee-batch-record-list">{(batch.records||[]).map(record=><button type="button" key={record.recordId} className="employee-batch-record-row" onClick={()=>onOpenRecord(record)}><div><strong>{en?record.employeeNameEn:record.employeeName}</strong><span>{record.employeeId} · {en?record.departmentEn:record.department}</span></div><div><span className={`status-badge ${record.resultStatus==='negative'||record.resultStatus==='cleared'?'active':''}`}>{record.resultStatus||'pending'}</span><b>›</b></div></button>)}</div></ObserverDialog>
}

function SubjectChooser({language,canEmployee,canEnvironmental,onClose,onPatient,onEmployee,onEnvironmental}){const en=language==='en';return <ObserverDialog eyebrow={en?'Surveillance':'Επιτήρηση'} title={en?'New surveillance':'Νέα επιτήρηση'} subtitle={en?'Choose the subject of the surveillance record.':'Επιλέξτε το αντικείμενο της επιτήρησης.'} width="wide" className="surveillance-subject-chooser" onClose={onClose}><div className="subject-choice-grid"><button type="button" onClick={onPatient}><span>01</span><strong>{en?'Patient':'Ασθενής'}</strong><small>{en?'Clinical surveillance episode':'Κλινικό επεισόδιο επιτήρησης'}</small></button>{canEmployee&&<button type="button" onClick={onEmployee}><span>02</span><strong>{en?'Employee':'Εργαζόμενος'}</strong><small>{en?'Individual or bulk employee screening':'Ατομικός ή μαζικός έλεγχος εργαζομένων'}</small></button>}{canEnvironmental&&<button type="button" onClick={onEnvironmental}><span>03</span><strong>{en?'Environment':'Περιβάλλον'}</strong><small>{en?'Water, surfaces or environmental sample':'Νερό, επιφάνειες ή περιβαλλοντικό δείγμα'}</small></button>}</div></ObserverDialog>}

function clickableRowProps(registry,id,onOpen){const rp=registry.rowProps(id,onOpen);return {...rp,className:`${rp.className} clickable-row`}}
function PatientRegistry({rows,totalRows,t,language,fmt,registry,openLinkedRecord}){const open=item=>openLinkedRecord(`/laboratory/${item._labSample?.id||patientLabCode(item.recordId||item.id)}`,String(item.id),rows.map(row=>String(row.id)));return <div className="scroll-table" ref={registry.scrollRef}>{totalRows?<RegistryTable bare
  columns={[{key:'patient',label:t('patient')},{key:'department',label:t('department')},{key:'started',label:t('clinicalRecords.startedAt')},{key:'microbiology',label:t('microbiology')},{key:'status',label:t('status')},{key:'reassessment',label:t('reassessment')}]}
  rows={rows} rowKey={item=>item.id}
  rowProps={item=>clickableRowProps(registry,String(item.id),()=>open(item))}
  renderRow={item=>{const state=reviewState(item);return <><td><strong>{language==='el'?item.patient:item.patientEn}</strong><small>{item.patientId||'—'}</small></td><td>{language==='el'?item.department:item.departmentEn}</td><td>{fmt(item.startedAt)}</td><td><strong>{latestOrganism(item)||'—'}</strong>{latestResistance(item)&&<small>{latestResistance(item)}</small>}</td><td><span className={`status-badge ${item.status==='active'?'active':''}`}>{item.status==='active'?t('clinicalRecords.activeSurveillanceState'):t('completed')}</span>{item.isolation?.status==='active'&&<small>{t('isolation')}</small>}</td><td><strong>{fmt(item.reviewDue)}</strong><small>{t(state)}</small></td></>}}
/>:<RegistryEmpty title={language==='en'?'No surveillance records':'Δεν υπάρχουν καταγραφές επιτήρησης'} text={language==='en'?'No surveillance records have been stored for this organization yet.':'Δεν έχουν καταχωριστεί ακόμη εγγραφές επιτήρησης για τον συγκεκριμένο οργανισμό.'}/>}</div>}

function UnifiedEmployeeRegistry({rows,totalRows,language,fmt,registry,onOpenRecord,onOpenBatch}){const en=language==='en';const rowId=item=>`${item._registryKind}-${item.recordId||item.id}`;const open=item=>{if(item._registryKind==='bulk'){onOpenBatch(item);return}onOpenRecord(item)};return <div className="scroll-table" ref={registry.scrollRef}>{totalRows?<RegistryTable bare
  columns={[{key:'employee',label:en?'Employee / group':'Εργαζόμενος / ομάδα'},{key:'department',label:en?'Department':'Τμήμα'},{key:'started',label:en?'Started':'Έναρξη'},{key:'screening',label:en?'Screening':'Έλεγχος'},{key:'result',label:en?'Result':'Αποτέλεσμα'},{key:'recheck',label:en?'Recheck':'Επανέλεγχος'}]}
  rows={rows} rowKey={rowId}
  rowProps={item=>clickableRowProps(registry,rowId(item),()=>open(item))}
  renderRow={item=>{const bulk=item._registryKind==='bulk';return <><td><strong>{bulk?(en?`Bulk surveillance · ${item.employeeCount||0} employees`:`Μαζική επιτήρηση · ${item.employeeCount||0} εργαζόμενοι`):(en?item.employeeNameEn:item.employeeName)}</strong>{!bulk&&<small>{item.employeeId||'—'}</small>}</td><td>{en?(item.departmentEn||item.department):(item.department||item.departmentEn)||'—'}</td><td>{fmt(item.startedAt)}</td><td>{(item.screeningTypes||[]).join(', ')||'—'}</td><td>{bulk?<><strong>{en?`${item.positiveCount||0} positive`:`${item.positiveCount||0} θετικές`}</strong><small>{en?`${item.activeCount||0} active`:`${item.activeCount||0} ενεργές`}</small></>:<span className={`status-badge ${item.resultStatus==='positive'?'active':''}`}>{item.resultStatus||'pending'}</span>}</td><td>{bulk?'—':<><strong>{fmt(item.recheckDue)}</strong><small>{item.interventionStatus||'—'}</small></>}</td></>}}
/>:<RegistryEmpty title={en?'No employee surveillance records':'Δεν υπάρχουν καταγραφές επιτήρησης εργαζομένων'} text={en?'Individual and bulk employee surveillance records will appear together here.':'Οι ατομικές και μαζικές επιτηρήσεις εργαζομένων εμφανίζονται μαζί εδώ.'}/>}</div>}

function EnvironmentalRegistry({rows,totalRows,language,t,fmt,registry,openLinkedRecord}){const en=language==='en';const open=item=>openLinkedRecord(`/laboratory/${item.id}`,String(item.id),rows.map(row=>String(row.id)));return <div className="scroll-table" ref={registry.scrollRef}>{totalRows?<RegistryTable bare
  columns={[{key:'source',label:en?'Type / source':'Τύπος / πηγή'},{key:'department',label:t('department')},{key:'collected',label:en?'Collected':'Συλλογή'},{key:'microbiology',label:t('microbiology')},{key:'status',label:t('status')},{key:'priority',label:en?'Priority':'Προτεραιότητα'}]}
  rows={rows} rowKey={item=>item.recordId||item.id}
  rowProps={item=>clickableRowProps(registry,String(item.id),()=>open(item))}
  renderRow={item=><><td><strong>{item.type?sampleTypeLabel(item.type,t):'—'}</strong><small>{item.source||'—'}</small></td><td>{item.department||'—'}</td><td>{fmt(item.collectedAt||item.requestedAt)}</td><td><strong>{item.organism||'—'}</strong>{item.resistance&&<small>{item.resistance}</small>}</td><td><span className={`status-badge ${item.status==='completed'?'':'active'}`}>{item.status||'—'}</span></td><td>{item.priority||'—'}</td></>}
/>:<RegistryEmpty title={en?'No environmental surveillance records':'Δεν υπάρχουν περιβαλλοντικές καταγραφές'} text={en?'Water, surface and environmental samples will appear here.':'Τα δείγματα νερού, επιφανειών και περιβάλλοντος θα εμφανίζονται εδώ.'}/>}</div>}

function RegistryEmpty({title,text}){return <div className="registry-empty-state"><strong>{title}</strong><span>{text}</span></div>}
function SummaryMetric({icon:Icon,label,value}){return <MetricCard className="summary-metric" icon={Icon} label={label} value={value}/>}
