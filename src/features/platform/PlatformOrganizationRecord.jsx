import { useEffect,useMemo,useState } from 'react'
import { Activity,Building2,KeyRound,LogIn,PauseCircle,Pencil,PlayCircle,Save,Send,Trash2,UserPlus,Users,X } from 'lucide-react'
import { EntityRecordShell } from '../../design-system/EntityRecordShell'
import { Button } from '../../design-system/Button'
import { IconButton } from '../../design-system/IconButton'
import { FilterBar } from '../../design-system/FilterBar'
import { RegistryPagination } from '../../design-system/RegistryPagination'
import { ObserverDialog,DialogActions } from '../../design-system/ObserverDialog'
import { LocationAutocompleteField } from '../../design-system/LocationAutocompleteField'
import { CITY_OPTIONS,COUNTRY_OPTIONS } from '../../core/reference/locationOptions'
import { useFeedback } from '../../core/feedback/FeedbackContext'
import { roleLabel } from '../../core/permissions/roleLabels'
import {
  createOrganizationUser,
  listOrganizationMembersDetailed,
  manageOrganizationUser,
  setPlatformOrganizationStatus,
  updatePlatformOrganization,
} from '../../core/tenant/tenantService'
import { HospitalDiagnosticsPanel } from './HospitalDiagnosticsPanel'
import { loadEmployeesAsync } from '../employees/employeeService'

const GREEK_REGIONS=['Ανατολική Μακεδονία και Θράκη','Κεντρική Μακεδονία','Δυτική Μακεδονία','Ήπειρος','Θεσσαλία','Ιόνια Νησιά','Δυτική Ελλάδα','Στερεά Ελλάδα','Αττική','Πελοπόννησος','Βόρειο Αιγαίο','Νότιο Αιγαίο','Κρήτη']
const HEALTH_REGIONS=['1η ΥΠΕ Αττικής','2η ΥΠΕ Πειραιώς και Αιγαίου','3η ΥΠΕ Μακεδονίας','4η ΥΠΕ Μακεδονίας και Θράκης','5η ΥΠΕ Θεσσαλίας και Στερεάς Ελλάδας','6η ΥΠΕ Πελοποννήσου, Ιονίων Νήσων, Ηπείρου και Δυτικής Ελλάδας','7η ΥΠΕ Κρήτης']
const ROLES=['hospital_admin','infection_control_lead','infection_control_member','department_manager','department_user','laboratory','committee_secretariat','hr_office','pharmacy','occupational_physician','doctor_reviewer','quality_manager','link_nurse','staff_user']

function Action({icon,tone,label,title,onClick,disabled=false}){return <div className="platform-org-action-item"><IconButton tone={tone} label={title} disabled={disabled} onClick={onClick}>{icon}</IconButton><span>{label}</span></div>}
function FormSection({title,subtitle,actions,children}){return <section className="platform-form-section"><header><div><strong>{title}</strong>{subtitle&&<span>{subtitle}</span>}</div>{actions&&<div className="platform-form-section-actions">{actions}</div>}</header>{children}</section>}

function toDraft(org){return {id:org.id,name:org.name||'',code:org.code||'',type:org.type||'hospital',status:org.status||'active',region:org.region||'',healthRegion:org.health_region||'',city:org.city||'',country:org.country||'',contactEmail:org.contact_email||'',contactPhone:org.contact_phone||'',bedCapacity:org.bed_capacity??''}}
function organizationTypeLabel(type,language='el'){
  const labels={hospital:{el:'Νοσοκομείο',en:'Hospital'},clinic:{el:'Κλινική',en:'Clinic'},group:{el:'Όμιλος',en:'Group'},other:{el:'Άλλο',en:'Other'}}
  return labels[type]?.[language==='en'?'en':'el']||type||'—'
}

export function PlatformOrganizationRecord({organization,language='el',initialTab='details',onTabChange,onBack,onEnter,onDelete,onChanged}){
  const en=language==='en',tx=(elText,enText)=>en?enText:elText
  const {notify,notifyError,confirm}=useFeedback()
  const [record,setRecord]=useState(organization)
  const [draft,setDraft]=useState(()=>toDraft(organization))
  const [users,setUsers]=useState([])
  const [employees,setEmployees]=useState([])
  const [loadingUsers,setLoadingUsers]=useState(true)
  const [saving,setSaving]=useState(false)
  const [working,setWorking]=useState(false)
  const [employeeLinkDrafts,setEmployeeLinkDrafts]=useState({})
  const [newAdmin,setNewAdmin]=useState({name:'',email:''})
  const [selectedUserId,setSelectedUserId]=useState('')
  const [editing,setEditing]=useState(false)
  const [userEditing,setUserEditing]=useState(false)
  const [userDraft,setUserDraft]=useState({fullName:'',email:'',phone:'',jobTitle:'',role:''})
  const [userQuery,setUserQuery]=useState('')
  const [userPage,setUserPage]=useState(1)
  const [userPageSize,setUserPageSize]=useState(15)
  const [createUserOpen,setCreateUserOpen]=useState(false)
  const [createUserDraft,setCreateUserDraft]=useState({fullName:'',email:'',role:'department_user'})

  useEffect(()=>{setRecord(organization);setDraft(toDraft(organization));setEditing(false)},[organization])
  useEffect(()=>{loadUsers()},[organization.id])

  async function loadUsers(){setLoadingUsers(true);try{const [rows,employeeRows]=await Promise.all([listOrganizationMembersDetailed(organization.id),loadEmployeesAsync(organization.id)]);setUsers(rows);setEmployees(employeeRows);setEmployeeLinkDrafts(Object.fromEntries(rows.map(user=>[user.userId,employeeRows.find(employee=>employee.userId===user.userId)?.dbId||''])));setSelectedUserId(current=>current&&rows.some(user=>user.userId===current)?current:'');return rows}catch(error){notifyError(error,'load',{operation:'platform_organization_users_load'});return []}finally{setLoadingUsers(false)}}
  const admins=useMemo(()=>users.filter(user=>user.role==='hospital_admin'),[users])
  const selectedUser=useMemo(()=>users.find(user=>user.userId===selectedUserId)||null,[users,selectedUserId])
  const selectedEmployee=useMemo(()=>selectedUser?employees.find(employee=>employee.userId===selectedUser.userId)||null:null,[employees,selectedUser])
  const employeeOptions=useMemo(()=>selectedUser?employees.filter(employee=>!employee.userId||employee.userId===selectedUser.userId):[],[employees,selectedUser])
  const filteredUsers=useMemo(()=>{const locale=language==='el'?'el-GR':'en-US',query=userQuery.trim().toLocaleLowerCase(locale);if(!query)return users;return users.filter(user=>[user.name,user.email,user.username,roleLabel(user.role,language)].some(value=>String(value||'').toLocaleLowerCase(locale).includes(query)))},[users,userQuery,language])
  const userTotalPages=Math.max(1,Math.ceil(filteredUsers.length/userPageSize)),safeUserPage=Math.min(userPage,userTotalPages),pagedUsers=filteredUsers.slice((safeUserPage-1)*userPageSize,safeUserPage*userPageSize)
  const canSave=Boolean(draft.name.trim()&&draft.code.trim())
  const createUserValid=Boolean(createUserDraft.fullName.trim().length>=2&&/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(createUserDraft.email.trim())&&createUserDraft.role)

  useEffect(()=>setUserPage(1),[userQuery,userPageSize])
  useEffect(()=>{if(userPage>userTotalPages)setUserPage(userTotalPages)},[userPage,userTotalPages])
  useEffect(()=>{
    if(!selectedUser){setUserEditing(false);return}
    setUserDraft({fullName:selectedUser.name||'',email:selectedUser.email||'',phone:selectedUser.phone||'',jobTitle:selectedUser.jobTitle||'',role:selectedUser.role||''})
    setUserEditing(false)
  },[selectedUserId,selectedUser?.name,selectedUser?.email,selectedUser?.phone,selectedUser?.jobTitle,selectedUser?.role])

  async function saveOrganization(){if(!canSave||saving)return;setSaving(true);try{const next=await updatePlatformOrganization(record.id,draft);setRecord(next);setDraft(toDraft(next));onChanged?.(next);setEditing(false);notify(tx('Τα στοιχεία του οργανισμού ενημερώθηκαν.','Organization details updated.'),'success',{operation:'platform_organization_update'})}catch(error){notifyError(error,'save',{operation:'platform_organization_update'})}finally{setSaving(false)}}
  async function togglePause(){if(working)return;const next=record.status==='suspended'?'active':'suspended';const ok=await confirm({title:next==='suspended'?tx('Παύση οργανισμού','Suspend organization'):tx('Ενεργοποίηση οργανισμού','Reactivate organization'),message:next==='suspended'?tx('Οι χρήστες του οργανισμού δεν θα μπορούν να λειτουργούν κανονικά μέχρι την επανενεργοποίηση.','Organization access will be suspended until reactivation.'):tx('Να ενεργοποιηθεί ξανά ο οργανισμός;','Reactivate this organization?'),confirmLabel:next==='suspended'?tx('Παύση','Suspend'):tx('Ενεργοποίηση','Reactivate')});if(!ok)return;setWorking(true);try{const updated=await setPlatformOrganizationStatus(record.id,next);setRecord(updated);setDraft(toDraft(updated));onChanged?.(updated);notify(next==='suspended'?tx('Ο οργανισμός τέθηκε σε παύση.','Organization suspended.'):tx('Ο οργανισμός ενεργοποιήθηκε.','Organization reactivated.'),'success',{operation:'platform_organization_status'})}catch(error){notifyError(error,'action',{operation:'platform_organization_status'})}finally{setWorking(false)}}
  async function resetAdmin(){const admin=admins[0];if(!admin||working)return;const invited=admin.status==='invited';const ok=await confirm({title:invited?tx('Επαναποστολή πρόσκλησης Hospital Admin','Resend Hospital Admin invitation'):tx('Επαναφορά κωδικού Hospital Admin','Reset Hospital Admin password'),message:invited?tx(`Η πρόσκληση θα σταλεί ξανά στο ${admin.email||'καταχωρημένο email'}.`,`The invitation will be sent again to ${admin.email||'the registered email'}.`):tx(`Θα σταλεί email επαναφοράς κωδικού στο ${admin.email||'καταχωρημένο email'}.`,`A password reset email will be sent to ${admin.email||'the registered email'}.`),confirmLabel:invited?tx('Επαναποστολή','Resend'):tx('Αποστολή email','Send email')});if(!ok)return;setWorking(true);try{await manageOrganizationUser({organizationId:record.id,userId:admin.userId,action:invited?'resend_invitation':'reset_password'});await loadUsers();notify(invited?tx('Η πρόσκληση επαναποστάλθηκε.','Invitation resent.'):tx('Στάλθηκε email επαναφοράς κωδικού.','Password reset email sent.'),'success',{operation:'platform_admin_account'})}catch(error){notifyError(error,'action',{operation:'platform_admin_account'})}finally{setWorking(false)}}
  async function inviteAdmin(){const name=newAdmin.name.trim(),email=newAdmin.email.trim();if(name.length<2||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||working)return;setWorking(true);try{const result=await createOrganizationUser({organizationId:record.id,fullName:name,role:'hospital_admin',email});if(!result?.emailSent)throw new Error(tx('Η πρόσκληση δεν μπόρεσε να αποσταλεί.','Invitation could not be sent.'));setNewAdmin({name:'',email:''});await loadUsers();notify(tx('Ο Hospital Admin δημιουργήθηκε και η πρόσκληση στάλθηκε.','Hospital Admin created and invitation sent.'),'success',{operation:'platform_admin_create'})}catch(error){notifyError(error,'save',{operation:'platform_admin_create'})}finally{setWorking(false)}}
  async function createOrganizationMember(){if(!createUserValid||working)return;const fullName=createUserDraft.fullName.trim(),email=createUserDraft.email.trim().toLowerCase(),role=createUserDraft.role;setWorking(true);try{const result=await createOrganizationUser({organizationId:record.id,fullName,role,email});const rows=await loadUsers();const created=rows.find(user=>String(user.email||'').toLowerCase()===email);setCreateUserOpen(false);setCreateUserDraft({fullName:'',email:'',role:'department_user'});if(created)setSelectedUserId(created.userId);if(result?.reused){notify(tx('Ο υπάρχων λογαριασμός προστέθηκε στον οργανισμό με τον επιλεγμένο ρόλο.','The existing account was added to this organization with the selected role.'),'success',{operation:'platform_user_create'})}else if(result?.emailSent){notify(tx('Ο χρήστης δημιουργήθηκε και η πρόσκληση στάλθηκε.','User created and invitation sent.'),'success',{operation:'platform_user_create'})}else{notify(tx('Ο χρήστης δημιουργήθηκε, αλλά η πρόσκληση δεν μπόρεσε να αποσταλεί.','User created, but the invitation could not be delivered.'),'warning',{operation:'platform_user_create'})}}catch(error){notifyError(error,'save',{operation:'platform_user_create'})}finally{setWorking(false)}}
  async function linkUserEmployee(user){const employeeDbId=employeeLinkDrafts[user.userId]||'';if(!employeeDbId||working)return;const employee=employees.find(item=>item.dbId===employeeDbId);if(!employee)return;const ok=await confirm({title:tx('Σύνδεση καρτέλας εργαζομένου','Link employee record'),message:tx(`Ο χρήστης ${user.name} θα συνδεθεί με την καρτέλα ${employee.lastName} ${employee.firstName} (${employee.id}).`,`User ${user.name} will be linked to employee record ${employee.firstName} ${employee.lastName} (${employee.id}).`),confirmLabel:tx('Σύνδεση','Link')});if(!ok)return;setWorking(true);try{await manageOrganizationUser({organizationId:record.id,userId:user.userId,action:'link_employee',employeeDbId});await loadUsers();notify(tx('Η καρτέλα εργαζομένου συνδέθηκε με τον λογαριασμό.','Employee record linked to the account.'),'success',{operation:'platform_user_employee_link'})}catch(error){notifyError(error,'save',{operation:'platform_user_employee_link'})}finally{setWorking(false)}}
  async function resetUserAccess(user){if(!user||working)return;const invited=user.status==='invited';const ok=await confirm({title:invited?tx('Επαναποστολή πρόσκλησης','Resend invitation'):tx('Επαναφορά κωδικού','Reset password'),message:invited?tx(`Η πρόσκληση θα σταλεί ξανά στο ${user.email||'καταχωρημένο email'}.`,`The invitation will be sent again to ${user.email||'the registered email'}.`):tx(`Θα σταλεί email επαναφοράς κωδικού στο ${user.email||'καταχωρημένο email'}.`,`A password reset email will be sent to ${user.email||'the registered email'}.`),confirmLabel:invited?tx('Επαναποστολή','Resend'):tx('Αποστολή email','Send email')});if(!ok)return;setWorking(true);try{await manageOrganizationUser({organizationId:record.id,userId:user.userId,action:invited?'resend_invitation':'reset_password'});await loadUsers();notify(invited?tx('Η πρόσκληση επαναποστάλθηκε.','Invitation resent.'):tx('Στάλθηκε email επαναφοράς κωδικού.','Password reset email sent.'),'success',{operation:'platform_user_access_reset'})}catch(error){notifyError(error,'action',{operation:'platform_user_access_reset'})}finally{setWorking(false)}}
  async function saveSelectedUser(){if(!selectedUser||working)return;const fullName=userDraft.fullName.trim(),email=userDraft.email.trim().toLowerCase();if(fullName.length<2||!/^\S+@\S+\.\S+$/.test(email)){notify(tx('Συμπλήρωσε έγκυρο ονοματεπώνυμο και email.','Enter a valid full name and email.'),'error');return}setWorking(true);try{await manageOrganizationUser({organizationId:record.id,userId:selectedUser.userId,action:'update',fullName,email,phone:userDraft.phone.trim()||null,jobTitle:userDraft.jobTitle.trim()||null,role:userDraft.role||selectedUser.role});await loadUsers();setUserEditing(false);notify(tx('Τα στοιχεία του χρήστη ενημερώθηκαν.','User details updated.'),'success',{operation:'platform_user_update'})}catch(error){notifyError(error,'save',{operation:'platform_user_update'})}finally{setWorking(false)}}
  async function deleteSelectedUser(){if(!selectedUser||working)return;const ok=await confirm({title:tx('Διαγραφή χρήστη','Delete user'),message:tx('Ο χρήστης '+selectedUser.name+' θα διαγραφεί οριστικά από το Limoxis Observer. Η ενέργεια δεν αναιρείται.','User '+selectedUser.name+' will be permanently deleted from Limoxis Observer. This action cannot be undone.'),confirmLabel:tx('Διαγραφή','Delete'),danger:true});if(!ok)return;setWorking(true);try{await manageOrganizationUser({organizationId:record.id,userId:selectedUser.userId,action:'delete'});setSelectedUserId('');await loadUsers();notify(tx('Ο χρήστης διαγράφηκε.','User deleted.'),'success',{operation:'platform_user_delete'})}catch(error){notifyError(error,'delete',{operation:'platform_user_delete'})}finally{setWorking(false)}}
  async function toggleSelectedUserStatus(){if(!selectedUser||working)return;const action=selectedUser.status==='disabled'?'reactivate':'suspend';const ok=await confirm({title:action==='suspend'?tx('Παύση χρήστη','Suspend user'):tx('Ενεργοποίηση χρήστη','Reactivate user'),message:action==='suspend'?tx('Ο χρήστης δεν θα μπορεί να συνδεθεί μέχρι να ενεργοποιηθεί ξανά.','The user will not be able to sign in until reactivated.'):tx('Να ενεργοποιηθεί ξανά ο χρήστης;','Reactivate this user?'),confirmLabel:action==='suspend'?tx('Παύση','Suspend'):tx('Ενεργοποίηση','Reactivate')});if(!ok)return;setWorking(true);try{await manageOrganizationUser({organizationId:record.id,userId:selectedUser.userId,action});await loadUsers();notify(action==='suspend'?tx('Ο χρήστης τέθηκε σε παύση.','User suspended.'):tx('Ο χρήστης ενεργοποιήθηκε.','User reactivated.'),'success',{operation:'platform_user_status'})}catch(error){notifyError(error,'action',{operation:'platform_user_status'})}finally{setWorking(false)}}

  const tabs=[{id:'details',label:tx('Στοιχεία & Ρυθμίσεις','Details & Settings'),icon:Building2},{id:'users',label:`${tx('Χρήστες & Ρόλοι','Users & Roles')} (${users.length})`,icon:Users},{id:'diagnostics',label:tx('Λειτουργία & Συμβάντα','Activity & Events'),icon:Activity}]
  const actions=<div className="platform-org-actions" aria-label={tx('Ενέργειες οργανισμού','Organization actions')}><Action icon={<LogIn size={18}/>} tone="primary" label={tx('Είσοδος','Enter')} title={tx('Είσοδος στον οργανισμό','Enter organization')} onClick={onEnter}/><Action icon={<KeyRound size={17}/>} tone="neutral" label="Hospital Admin" title={tx('Επαναφορά κωδικού ή πρόσκλησης Hospital Admin','Reset Hospital Admin password or invitation')} disabled={!admins.length||working} onClick={resetAdmin}/><Action icon={record.status==='suspended'?<PlayCircle size={17}/>:<PauseCircle size={17}/>} tone={record.status==='suspended'?'success':'neutral'} label={record.status==='suspended'?tx('Ενεργοποίηση','Reactivate'):tx('Παύση','Suspend')} title={record.status==='suspended'?tx('Ενεργοποίηση οργανισμού','Reactivate organization'):tx('Παύση οργανισμού','Suspend organization')} disabled={working} onClick={togglePause}/><Action icon={<Trash2 size={17}/>} tone="danger" label={tx('Διαγραφή','Delete')} title={tx('Οριστική διαγραφή οργανισμού','Delete organization permanently')} disabled={working} onClick={onDelete}/></div>

  if(initialTab==='users'&&selectedUser){const initials=(selectedUser.name||'').split(/\s+/).filter(Boolean).slice(0,2).map(part=>part[0]).join('').toUpperCase()||'U';return <EntityRecordShell className="platform-user-record-shell workspace-fill" avatar={initials} eyebrow={tx('ΧΡΗΣΤΗΣ ΟΡΓΑΝΙΣΜΟΥ','ORGANIZATION USER')} title={selectedUser.name} subtitle={(selectedUser.email||'—')+' · '+(selectedUser.username||'—')} status={<span className={'status-badge '+(selectedUser.status==='active'?'active':selectedUser.status==='disabled'?'danger':'temporary')}>{selectedUser.status==='active'?tx('Ενεργός','Active'):selectedUser.status==='disabled'?tx('Σε παύση','Suspended'):tx('Εκκρεμής','Pending')}</span>} onBack={()=>setSelectedUserId('')} backLabel={tx('Πίσω','Back')} headerActions={<div className="platform-user-record-header-actions">{!userEditing?<Button variant="secondary" onClick={()=>setUserEditing(true)}><Pencil size={15}/>{tx('Επεξεργασία','Edit')}</Button>:<><Button variant="secondary" onClick={()=>{setUserDraft({fullName:selectedUser.name||'',email:selectedUser.email||'',phone:selectedUser.phone||'',jobTitle:selectedUser.jobTitle||'',role:selectedUser.role||''});setUserEditing(false)}}><X size={15}/>{tx('Ακύρωση','Cancel')}</Button><Button disabled={working} onClick={saveSelectedUser}><Save size={15}/>{tx('Αποθήκευση','Save')}</Button></>}<Button variant="danger" disabled={working} onClick={deleteSelectedUser}><Trash2 size={15}/>{tx('Διαγραφή','Delete')}</Button></div>}><div className="platform-user-record-workspace"><section className="platform-user-record-section"><div className="platform-user-record-section-heading"><div><strong>{tx('Στοιχεία χρήστη','User details')}</strong><span>{tx('Στοιχεία ταυτότητας και επικοινωνίας του λογαριασμού.','Identity and contact details for this account.')}</span></div></div><div className="platform-user-record-grid"><label className="field"><span>{tx('Ονοματεπώνυμο','Full name')}</span><input value={userDraft.fullName} readOnly={!userEditing} onChange={e=>setUserDraft(x=>({...x,fullName:e.target.value}))}/></label><label className="field"><span>Email</span><input type="email" value={userDraft.email} readOnly={!userEditing} onChange={e=>setUserDraft(x=>({...x,email:e.target.value}))}/></label><label className="field"><span>Username</span><input value={selectedUser.username||'—'} readOnly /></label><label className="field"><span>{tx('Τηλέφωνο','Phone')}</span><input value={userDraft.phone} readOnly={!userEditing} onChange={e=>setUserDraft(x=>({...x,phone:e.target.value}))}/></label><label className="field"><span>{tx('Θέση / Ιδιότητα','Job title')}</span><input value={userDraft.jobTitle} readOnly={!userEditing} onChange={e=>setUserDraft(x=>({...x,jobTitle:e.target.value}))}/></label><label className="field"><span>{tx('Ρόλος','Role')}</span><select value={userDraft.role||selectedUser.role} disabled={!userEditing} onChange={e=>setUserDraft(x=>({...x,role:e.target.value}))}>{ROLES.map(role=><option key={role} value={role}>{roleLabel(role,language)}</option>)}</select></label></div></section><section className="platform-user-record-section"><div className="platform-user-record-section-heading"><div><strong>{tx('Καρτέλα εργαζομένου','Employee record')}</strong><span>{selectedEmployee?tx('Συνδεδεμένη με '+selectedEmployee.lastName+' '+selectedEmployee.firstName+' ('+selectedEmployee.id+').','Linked to '+selectedEmployee.firstName+' '+selectedEmployee.lastName+' ('+selectedEmployee.id+').'):tx('Δεν έχει συνδεθεί καρτέλα εργαζομένου.','No employee record is linked.')}</span></div></div><div className="platform-user-record-link-row"><select className="platform-role-select" value={employeeLinkDrafts[selectedUser.userId]||''} disabled={!userEditing} onChange={e=>setEmployeeLinkDrafts(x=>({...x,[selectedUser.userId]:e.target.value}))}><option value="">{tx('Επιλογή εργαζομένου…','Select employee…')}</option>{employeeOptions.map(employee=><option key={employee.dbId} value={employee.dbId}>{employee.lastName+' '+employee.firstName+' · '+employee.id+(employee.department?' · '+employee.department:'')}</option>)}</select><Button variant="secondary" disabled={!userEditing||working||!employeeLinkDrafts[selectedUser.userId]||selectedEmployee?.dbId===employeeLinkDrafts[selectedUser.userId]} onClick={()=>linkUserEmployee(selectedUser)}>{tx(selectedEmployee?'Αλλαγή σύνδεσης':'Σύνδεση καρτέλας',selectedEmployee?'Change link':'Link record')}</Button></div></section><section className="platform-user-record-section platform-user-record-security"><div className="platform-user-record-section-heading"><div><strong>{tx('Πρόσβαση & ασφάλεια','Access & security')}</strong><span>{tx('Διαχείριση κατάστασης λογαριασμού και ανάκτησης πρόσβασης.','Manage account status and access recovery.')}</span></div></div><div className="platform-user-record-actions"><Button variant="secondary" disabled={working} onClick={()=>resetUserAccess(selectedUser)}><KeyRound size={15}/>{selectedUser.status==='invited'?tx('Επαναποστολή πρόσκλησης','Resend invitation'):tx('Επαναφορά κωδικού','Reset password')}</Button><Button variant="secondary" disabled={working} onClick={toggleSelectedUserStatus}>{selectedUser.status==='disabled'?<PlayCircle size={15}/>:<PauseCircle size={15}/>} {selectedUser.status==='disabled'?tx('Ενεργοποίηση','Reactivate'):tx('Παύση','Suspend')}</Button></div></section></div></EntityRecordShell>}

  return <><EntityRecordShell className="platform-owner-record-shell platform-organization-record-workspace" avatar={<Building2 size={20}/>} eyebrow={tx('ΚΑΡΤΕΛΑ ΟΡΓΑΝΙΣΜΟΥ','ORGANIZATION RECORD')} title={draft.name||record.name} subtitle={`${record.code||'—'} · ${draft.city||'—'} · ${draft.region||'—'}`} status={<span className={`status-badge ${record.status==='active'?'active':'danger'}`}>{record.status==='active'?tx('Ενεργός','Active'):tx('Σε παύση','Suspended')}</span>} headerActions={actions} tabs={tabs} activeTab={initialTab} onTabChange={onTabChange} onBack={onBack} backLabel={tx('Πίσω','Back')}>
    {initialTab==='details'&&<div className="platform-owner-details platform-organization-record-form"><div className="platform-organization-summary-strip"><div><span>{tx('Τύπος','Type')}</span><strong>{organizationTypeLabel(record.type,language)}</strong></div><div><span>{tx('Χρήστες','Users')}</span><strong>{users.length}</strong></div><div><span>Hospital Admin</span><strong>{admins.length||0}</strong></div><div><span>{tx('Κλίνες','Beds')}</span><strong>{record.bed_capacity??'—'}</strong></div></div><div className={`platform-record-edit-fieldset ${editing?'is-editing':'is-locked'}`}><div className="platform-form-shell"><FormSection title={tx('Ταυτότητα οργανισμού','Organization identity')} actions={editing?<><Button variant="secondary" onClick={()=>{setDraft(toDraft(record));setEditing(false)}} disabled={saving}><X size={15}/>{tx('Ακύρωση','Cancel')}</Button><Button onClick={saveOrganization} disabled={!canSave||saving||working}><Save size={15}/>{saving?tx('Αποθήκευση…','Saving…'):tx('Αποθήκευση','Save')}</Button></>:<IconButton tone="edit" label={tx('Επεξεργασία','Edit')} onClick={()=>setEditing(true)}><Pencil size={16}/></IconButton>}><div className="platform-form-grid"><label className="field field-wide"><span>{tx('Επωνυμία','Name')} *</span><input value={draft.name} readOnly={!editing} onChange={e=>setDraft(x=>({...x,name:e.target.value}))}/></label><label className="field"><span>{tx('Κωδικός / Hospital Prefix','Code / Hospital Prefix')}</span><input value={draft.code} readOnly/></label><label className="field"><span>{tx('Τύπος','Type')}</span><select value={draft.type} disabled={!editing} onChange={e=>setDraft(x=>({...x,type:e.target.value}))}><option value="hospital">{tx('Νοσοκομείο','Hospital')}</option><option value="clinic">{tx('Κλινική','Clinic')}</option><option value="group">{tx('Όμιλος','Group')}</option><option value="other">{tx('Άλλο','Other')}</option></select></label></div></FormSection><FormSection title={tx('Τοποθεσία & λειτουργία','Location & operations')}><div className="platform-form-grid"><label className="field"><span>{tx('Περιφέρεια','Region')}</span><select value={draft.region} disabled={!editing} onChange={e=>setDraft(x=>({...x,region:e.target.value}))}><option value="">—</option>{GREEK_REGIONS.map(item=><option key={item}>{item}</option>)}</select></label><label className="field field-wide"><span>{tx('Υγειονομική Περιφέρεια (ΥΠΕ)','Health Region')}</span><select value={draft.healthRegion} disabled={!editing} onChange={e=>setDraft(x=>({...x,healthRegion:e.target.value}))}><option value="">—</option>{HEALTH_REGIONS.map(item=><option key={item}>{item}</option>)}</select></label><LocationAutocompleteField label={tx('Πόλη','City')} value={draft.city} onChange={value=>setDraft(x=>({...x,city:value}))} options={CITY_OPTIONS} disabled={!editing}/><LocationAutocompleteField label={tx('Χώρα','Country')} value={draft.country} onChange={value=>setDraft(x=>({...x,country:value}))} options={COUNTRY_OPTIONS} disabled={!editing}/><label className="field"><span>{tx('Δυναμικότητα κλινών','Bed capacity')}</span><input type="number" min="0" value={draft.bedCapacity} readOnly={!editing} onChange={e=>setDraft(x=>({...x,bedCapacity:e.target.value}))}/></label></div></FormSection><FormSection title={tx('Στοιχεία επικοινωνίας','Contact details')}><div className="platform-form-grid"><label className="field field-wide"><span>Email</span><input type="email" value={draft.contactEmail} readOnly={!editing} onChange={e=>setDraft(x=>({...x,contactEmail:e.target.value}))}/></label><label className="field"><span>{tx('Τηλέφωνο','Phone')}</span><input value={draft.contactPhone} readOnly={!editing} onChange={e=>setDraft(x=>({...x,contactPhone:e.target.value}))}/></label></div></FormSection><FormSection title="Hospital Admin" subtitle={tx('Ο Hospital Admin μπορεί να οριστεί ή να αλλάξει και από την καρτέλα Χρήστες & Ρόλοι.','The Hospital Admin can also be assigned or changed from Users & Roles.')} >{loadingUsers?<div className="inline-empty">{tx('Φόρτωση…','Loading…')}</div>:admins.length?<div className="platform-admin-summary">{admins.map(admin=><div key={admin.userId} className="platform-admin-summary-row"><div><strong>{admin.name}</strong><span>{admin.email||'—'} · {admin.username}</span></div><span className={`status-badge ${admin.status==='active'?'active':admin.status==='disabled'?'danger':'temporary'}`}>{admin.status==='active'?tx('Ενεργός','Active'):admin.status==='disabled'?tx('Σε παύση','Suspended'):tx('Εκκρεμής','Pending')}</span></div>)}</div>:<div className="platform-admin-create"><div className="platform-form-grid"><label className="field"><span>{tx('Ονοματεπώνυμο νέου Hospital Admin','New Hospital Admin full name')}</span><input value={newAdmin.name} onChange={e=>setNewAdmin(x=>({...x,name:e.target.value}))}/></label><label className="field field-wide"><span>{tx('Email πρόσκλησης','Invitation email')}</span><input type="email" value={newAdmin.email} onChange={e=>setNewAdmin(x=>({...x,email:e.target.value}))}/></label></div><Button variant="secondary" disabled={working||newAdmin.name.trim().length<2||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(newAdmin.email.trim())} onClick={inviteAdmin}><Send size={15}/>{tx('Ορισμός Hospital Admin & πρόσκληση','Assign Hospital Admin & invite')}</Button></div>}</FormSection></div></div></div>}

    {initialTab==='users'&&<div className="platform-owner-users workspace-column workspace-fill"><div className="platform-user-role-help section-toolbar"><div><strong>{tx('Χρήστες & Ρόλοι','Users & Roles')}</strong><span>{tx('Επίλεξε έναν χρήστη για πλήρη διαχείριση λογαριασμού, ρόλου και σύνδεσης με εργαζόμενο.','Select a user to manage the account, role and employee link.')}</span></div><Button onClick={()=>{setCreateUserDraft({fullName:'',email:'',role:'department_user'});setCreateUserOpen(true)}}><UserPlus size={15}/>{tx('Νέος χρήστης','New user')}</Button></div><section className="surface registry-workspace workspace-column workspace-fill"><FilterBar query={userQuery} onQueryChange={setUserQuery} placeholder={tx('Αναζήτηση χρήστη, email ή ρόλου…','Search user, email or role…')} />{loadingUsers?<div className="inline-empty">{tx('Φόρτωση χρηστών…','Loading users…')}</div>:filteredUsers.length?<><div className="scroll-table"><table className="data-table sticky-table"><thead><tr><th>{tx('Χρήστης','User')}</th><th>Username</th><th>{tx('Ρόλος','Role')}</th><th>{tx('Κατάσταση','Status')}</th></tr></thead><tbody>{pagedUsers.map(user=><tr key={user.userId} tabIndex={0} className="platform-owner-clickable-row" onClick={()=>setSelectedUserId(user.userId)} onKeyDown={event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();setSelectedUserId(user.userId)}}}><td><strong>{user.name}</strong><small>{user.email||'—'}</small></td><td>{user.username}</td><td>{roleLabel(user.role,language)}</td><td><span className={'status-badge '+(user.status==='active'?'active':user.status==='disabled'?'danger':'temporary')}>{user.status==='active'?tx('Ενεργός','Active'):user.status==='disabled'?tx('Σε παύση','Suspended'):tx('Εκκρεμής','Pending')}</span></td></tr>)}</tbody></table></div><RegistryPagination language={language} page={safeUserPage} totalPages={userTotalPages} totalItems={filteredUsers.length} pageSize={userPageSize} onPageChange={setUserPage} onPageSizeChange={setUserPageSize}/></>:<div className="registry-empty-state"><strong>{users.length?tx('Δεν βρέθηκαν χρήστες για την αναζήτηση.','No users match the search.'):tx('Δεν υπάρχουν χρήστες.','No users.')}</strong><span>{users.length?'':tx('Δημιούργησε τον πρώτο χρήστη από το κουμπί Νέος χρήστης.','Create the first user with the New user button.')}</span></div>}</section></div>}
    {initialTab==='diagnostics'&&<HospitalDiagnosticsPanel organization={record} language={language}/>} 
  </EntityRecordShell>

  {createUserOpen&&<ObserverDialog eyebrow={tx('ΧΡΗΣΤΕΣ & ΡΟΛΟΙ','USERS & ROLES')} title={tx('Νέος χρήστης','New user')} subtitle={tx(`Πρόσθεσε χρήστη στον οργανισμό ${record.name}. Θα σταλεί πρόσκληση στο email του.`,`Add a user to ${record.name}. An invitation will be sent to their email.`)} onClose={()=>!working&&setCreateUserOpen(false)} width="compact" footer={<DialogActions showCancel onCancel={()=>setCreateUserOpen(false)} onSave={createOrganizationMember} saveLabel={working?tx('Αποστολή…','Sending…'):tx('Δημιουργία & πρόσκληση','Create & invite')} disabled={!createUserValid||working}/> }><div className="platform-form-shell"><div className="platform-form-grid"><label className="field field-wide"><span>{tx('Ονοματεπώνυμο','Full name')} *</span><input autoFocus value={createUserDraft.fullName} onChange={e=>setCreateUserDraft(x=>({...x,fullName:e.target.value}))}/></label><label className="field field-wide"><span>Email *</span><input type="email" value={createUserDraft.email} onChange={e=>setCreateUserDraft(x=>({...x,email:e.target.value}))}/></label><label className="field field-wide"><span>{tx('Ρόλος','Role')} *</span><select value={createUserDraft.role} onChange={e=>setCreateUserDraft(x=>({...x,role:e.target.value}))}>{ROLES.map(role=><option key={role} value={role}>{roleLabel(role,language)}</option>)}</select></label></div></div></ObserverDialog>}
  </>
}
