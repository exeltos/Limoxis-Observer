import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { Button } from '../../design-system/Button';import { supabase } from '../../core/supabase/client';export function ResetPasswordPage(){const nav=useNavigate(),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[saving,setSaving]=useState(false);const valid=password.length>=12&&/[A-Z]/.test(password)&&/[a-z]/.test(password)&&/\d/.test(password)&&/[^A-Za-z0-9]/.test(password)&&password===confirm;async function submit(e){e.preventDefault();if(!valid)return;setSaving(true);const {error}=await supabase.auth.updateUser({password});if(error)setError(error.message);else nav('/login',{replace:true});setSaving(false)}return <div className="auth-layout"><section className="auth-brand-panel"><div className="auth-value"><span className="eyebrow">RESET PASSWORD</span><h1>Ορισμός νέου κωδικού.</h1></div></section><section className="auth-form-panel"><form className="login-card" onSubmit={submit}><div className="login-heading"><h2>Νέος κωδικός</h2><p>12+ χαρακτήρες, κεφαλαίο, πεζό, αριθμός και σύμβολο.</p></div><label className="field"><span>Νέος κωδικός</span><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label className="field"><span>Επιβεβαίωση</span><input className="input" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{error&&<div className="form-error">{error}</div>}<Button type="submit" disabled={!valid||saving}>{saving?'Αποθήκευση…':'Αλλαγή κωδικού'}</Button></form></section></div>}
