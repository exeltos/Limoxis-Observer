# v0.47.5 — Unified Registry

Patients and Laboratory now use the same canonical `registry-workspace canonical-paginated-registry` shell.
Top-level paginated registries no longer create an internal vertical scrollbar. Pagination stays pinned to the bottom. Horizontal overflow remains available only when needed.
The old dedicated patient/lab shell geometry is no longer used by these pages.
