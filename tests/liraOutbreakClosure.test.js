import {describe,expect,it} from 'vitest'
import {buildOutbreakClosureReadiness} from '../src/features/lira/liraOutbreakClosure'
import fs from 'node:fs'
describe('LIRA outbreak closure readiness',()=>{it('blocks unresolved work',()=>{const r=buildOutbreakClosureReadiness({lineList:{rows:[{recordKey:'a'}]},actions:[{status:'open',due_date:'2026-01-01'}],events:[{event_type:'hypothesis',created_at:'2026-01-01'}],today:'2026-09-22'});expect(r.ready).toBe(false);expect(r.counts.overdueActions).toBe(1);expect(r.blockers.map(x=>x.code)).toEqual(['unreviewed_candidates','open_actions','unresolved_hypotheses'])});it('requires human closure after deterministic readiness',()=>{const r=buildOutbreakClosureReadiness({lineList:{rows:[{recordKey:'a'}]},caseReviews:[{record_key:'a',classification:'confirmed',reviewed_at:'2026-09-20'}],actions:[{status:'closed'}],events:[{event_type:'hypothesis',created_at:'2026-09-19'},{event_type:'decision',created_at:'2026-09-20'}]});expect(r.ready).toBe(true);expect(r.guardrails).toEqual({humanClosureRequired:true,automaticClosure:false})});it('UI requires closure rationale',()=>{const ui=fs.readFileSync('src/features/management/LiraOutbreakInvestigationsPanel.jsx','utf8');expect(ui).toContain('closureNote.trim()');expect(ui).toContain('human readiness review')})})
