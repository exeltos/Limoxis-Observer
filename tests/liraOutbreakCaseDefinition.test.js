import {describe,expect,it} from 'vitest'
import {buildOutbreakLineList,buildOutbreakTimeline,evaluateOutbreakCaseDefinition} from '../src/features/lira/liraOutbreakInvestigation.js'
const data={laboratory:[{resultId:'r1',patientId:'p1',department:'ΜΕΘ',organism:'Candida auris',specimenType:'blood',collectedAt:'2026-09-10'}],haiClassifications:[],devices:[],isolations:[]}
describe('LIRA outbreak timeline and case definition',()=>{const list=buildOutbreakLineList(data);it('builds chronological observed timeline',()=>{expect(buildOutbreakTimeline(list)[0]).toMatchObject({date:'2026-09-10',type:'microbiology',recordKey:'r1'})});it('requires definition id and version',()=>{const x=evaluateOutbreakCaseDefinition(list,{organism:'Candida auris'});expect(x.cases[0].classification).toBe('unclassified');expect(x.guardrails.requiresVersionedDefinition).toBe(true)});it('can identify explicit criteria without confirming a case',()=>{const x=evaluateOutbreakCaseDefinition(list,{id:'local-cauris-investigation',version:'1.0',organism:'Candida auris',department:'ΜΕΘ',from:'2026-09-01',to:'2026-09-30'});expect(x.cases[0]).toMatchObject({classification:'meets_machine_readable_criteria',humanValidationRequired:true,finalClassification:null});expect(x.guardrails).toMatchObject({autonomousConfirmedCase:false,clinicalDiagnosis:false})})})
