import {describe,expect,it} from 'vitest'
import {buildInvestigationCopilot} from '../src/features/lira/liraInvestigationCopilot'
import fs from 'node:fs'
describe('LIRA investigation copilot',()=>{it('prioritizes governed pending work',()=>{const c=buildInvestigationCopilot({investigation:{status:'active'},lineList:{rows:[{recordKey:'a'}],summary:{uniquePatients:1}},caseReviews:[],actions:[{status:'open',due_date:'2026-01-01'}],events:[],closure:{blockers:[{code:'unresolved_hypotheses'}]},language:'en'});expect(c.suggestions.map(x=>x.id)).toEqual(['review_cases','overdue_capa','hypotheses']);expect(c.guardrails.noAutonomousAction).toBe(true)});it('offers closure only when governed readiness is complete',()=>{const c=buildInvestigationCopilot({investigation:{status:'active'},lineList:{rows:[],summary:{}},closure:{ready:true,blockers:[]}});expect(c.suggestions[0].id).toBe('closure')});it('is embedded in selected investigation workspace',()=>{const ui=fs.readFileSync('src/features/management/LiraOutbreakInvestigationsPanel.jsx','utf8');expect(ui).toContain('buildInvestigationCopilot');expect(ui).toContain('Bound to this investigation');expect(ui).toContain('runCopilotSuggestion')})})
