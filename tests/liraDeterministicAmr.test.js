import {describe,expect,it} from 'vitest'
import {buildDeterministicAmrAnalysis} from '../src/features/lira/liraAmrAnalysis.js'
const rows=[{resultId:'r1',patientId:'p1',organism:'Klebsiella pneumoniae',department:'ΜΕΘ',collectedAt:'2026-09-01',validationStatus:'validated',ast:[{antimicrobialName:'Meropenem',sirCategory:'R',breakpointStandard:'EUCAST',breakpointVersion:'16.0'}]},{resultId:'r2',patientId:'p1',organism:'Klebsiella pneumoniae',department:'ΜΕΘ',collectedAt:'2026-09-03',validationStatus:'validated',ast:[{antimicrobialName:'Meropenem',sirCategory:'S',breakpointStandard:'EUCAST',breakpointVersion:'16.0'}]},{resultId:'r3',patientId:'p2',organism:'Klebsiella pneumoniae',department:'ΜΕΘ',collectedAt:'2026-09-04',validationStatus:'validated',ast:[{antimicrobialName:'Meropenem',sirCategory:'S',breakpointStandard:'EUCAST',breakpointVersion:'16.0'}]}]
describe('deterministic AMR analysis',()=>{it('deduplicates first patient-organism isolate',()=>{const x=buildDeterministicAmrAnalysis(rows,{organism:'klebsiella'});expect(x.includedIsolates).toBe(2);expect(x.results[0]).toMatchObject({tested:2,resistant:1,resistancePercent:50})});it('preserves breakpoint provenance',()=>{expect(buildDeterministicAmrAnalysis(rows).results[0].breakpointVersions).toEqual(['EUCAST 16.0'])});it('suppresses configured small samples without inventing a threshold',()=>{const x=buildDeterministicAmrAnalysis(rows,{minimumIsolates:3});expect(x.results[0].suppressed).toBe(true);expect(x.results[0].resistancePercent).toBeNull()});it('does not recalculate SIR',()=>{expect(buildDeterministicAmrAnalysis(rows).guardrails).toMatchObject({recalculatesSir:false,usesRecordedSir:true})})})
