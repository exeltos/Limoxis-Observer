import {describe,expect,it} from 'vitest'
import {buildAmrChangeSignals,buildAmrConcentrationSignals} from '../src/features/lira/liraAmrSignals.js'
const row=(id,date,department,sir,patientId)=>({id,resultId:id,patientId,department,organism:'Klebsiella pneumoniae',collectedAt:date,validationStatus:'validated',ast:[{antimicrobialName:'Meropenem',antimicrobialCode:'MEM',sirCategory:sir,breakpointStandard:'EUCAST',breakpointVersion:'16.0'}]})
describe('LIRA AMR trend and concentration signals',()=>{const rows=[row('a','2026-07-01','ΜΕΘ','S','p1'),row('b','2026-07-02','ΜΕΘ','S','p2'),row('c','2026-08-01','ΜΕΘ','R','p3'),row('d','2026-08-02','ΜΕΘ','S','p4'),row('e','2026-08-03','Παθολογική','R','p5')];it('reports percentage-point change without outbreak declaration',()=>{const x=buildAmrChangeSignals(rows,{from:'2026-08-01',to:'2026-08-31'},{from:'2026-07-01',to:'2026-07-31'},{department:'ΜΕΘ',organism:'klebsiella',antimicrobial:'meropenem'});expect(x.signals[0]).toMatchObject({percentagePointChange:50,direction:'increase',outbreak:false});expect(x.guardrails.outbreakDeclaration).toBe(false)});it('keeps spatial concentration descriptive',()=>{const x=buildAmrConcentrationSignals(rows,{from:'2026-08-01',to:'2026-08-31',organism:'klebsiella',antimicrobial:'meropenem'});expect(x.distributions.map(d=>d.department)).toContain('ΜΕΘ');expect(x.guardrails).toMatchObject({outbreakDeclaration:false,transmissionInference:false,thresholdInvented:false})});it('does not create a change signal without comparable periods',()=>{const x=buildAmrChangeSignals(rows,{from:'2026-08-01',to:'2026-08-31'},{from:'2026-06-01',to:'2026-06-30'},{department:'ΜΕΘ',organism:'klebsiella'});expect(x.signals).toHaveLength(0)})})
