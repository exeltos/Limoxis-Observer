import {describe,expect,it} from 'vitest'
import {buildOutbreakLineList,buildOutbreakEvidenceTrail} from '../src/features/lira/liraOutbreakInvestigation.js'
const data={laboratory:[{resultId:'r1',patientId:'p1',department:'ΜΕΘ',organism:'Klebsiella pneumoniae',specimenType:'blood',resistance:'CRE',collectedAt:'2026-09-10'}],haiClassifications:[{id:'h1',patientId:'p1',department:'ΜΕΘ',haiType:'CLABSI',classifiedAt:'2026-09-11',definitionSet:'NHSN',definitionVersion:'2026'}],devices:[{id:'d1',patientId:'p1',department:'ΜΕΘ',deviceType:'central_line',insertedAt:'2026-09-01',removedAt:'2026-09-12'}],isolations:[{id:'i1',patientId:'p1',department:'ΜΕΘ',startedAt:'2026-09-09',endedAt:null,status:'active'}]}
describe('LIRA outbreak investigation line list',()=>{it('organizes person place time and linked evidence',()=>{const x=buildOutbreakLineList(data,{from:'2026-09-01',to:'2026-09-30',department:'ΜΕΘ',organism:'klebsiella'});expect(x.summary).toMatchObject({records:1,uniquePatients:1,departments:['ΜΕΘ']});expect(x.rows[0]).toMatchObject({facts:{person:true,place:true,time:true},transmissionConfirmed:false,outbreakConfirmed:false});expect(x.rows[0].devices).toHaveLength(1);expect(x.rows[0].isolation).toHaveLength(1)});it('keeps hypotheses separate from facts',()=>{const x=buildOutbreakLineList(data);expect(x.rows[0].hypothesis).toBeNull();expect(x.guardrails).toMatchObject({outbreakDeclaration:false,transmissionInference:false,missingExposureInference:false,hypothesisAsFact:false})});it('builds traceable evidence trail',()=>{const x=buildOutbreakEvidenceTrail(buildOutbreakLineList(data));expect(x[0].evidence.map(e=>e.source)).toEqual(['microbiology_results','hai_classifications','surveillance_devices','isolation_episodes'])})})
