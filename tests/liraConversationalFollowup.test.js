import {describe,expect,it} from 'vitest'
import {interpretLiraQuestion,LIRA_INTENTS,LIRA_TOPICS} from '../src/features/lira/liraQuestionModel'
import fs from 'node:fs'
describe('LIRA conversational follow-ups',()=>{it('keeps AMR context for department drill-down',()=>{const plan=interpretLiraQuestion('σε ποια τμήματα έχω μικρόβια;',{previousPlan:{topic:LIRA_TOPICS.AMR,intent:'overview',department:'all',periodDays:0}});expect(plan.topic).toBe(LIRA_TOPICS.AMR);expect(plan.intent).toBe(LIRA_INTENTS.FOLLOW_UP);expect(plan.followUp).toBe(true)});it('recognizes organism drill-down',()=>{const plan=interpretLiraQuestion('ποια μικρόβια είναι αυτά;',{previousPlan:{topic:LIRA_TOPICS.LABORATORY,intent:'overview'}});expect(plan.intent).toBe(LIRA_INTENTS.FOLLOW_UP);expect(plan.topic).toBe(LIRA_TOPICS.LABORATORY)});it('has deterministic follow-up branches before generic overview',()=>{const ui=fs.readFileSync('src/features/lira/LiraAssistantLauncher.jsx','utf8');expect(ui).toContain('Departments in the previous result');expect(ui.indexOf('asksDepartments')).toBeLessThan(ui.indexOf("if(plan.topic===LIRA_TOPICS.GENERAL)"))})})
