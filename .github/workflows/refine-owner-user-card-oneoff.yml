name: Refine owner user card one off
on:
  push:
    paths:
      - .github/workflows/refine-owner-user-card-oneoff.yml
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/refine-owner-user-card.mjs
      - run: |
          git config user.name "Exeltos"
          git config user.email "info@exeltos.com"
          git add src/features/platform/PlatformOrganizationRecord.jsx src/styles/design-system-layouts.css
          git commit -m "fix(owner): refine user record and edit affordance" || exit 0
          git push
